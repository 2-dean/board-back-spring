<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.board.repository.BoardRepository">
    <insert id="save" parameterType="Board">
        INSERT INTO BOARD (TITLE, CONTENT, NAME)
        VALUES (#{title}, #{content}, #{name})
    </insert>

    <select id="getBoardList" resultType="Board">
        SELECT idx, title, content, name, save_date, modify_date
        FROM BOARD
    </select>

    <select id="getBoardOne" parameterType="int" resultType="Board">
        SELECT idx, title, content, name, save_date, modify_date
        FROM BOARD
        WHERE BNUM = #{bnum}
    </select>


    <!-- 검색 -->
    <select id="findBoardByTitle" parameterType="String" resultType="Board">
        SELECT *
        FROM BOARD
        WHERE TITLE = #{title}
    </select>

    <select id="findBoardByWriter" parameterType="String" resultType="Board">
        SELECT *
        FROM BOARD
        WHERE NAME = #{name}
    </select>

    <update id="modifyBoard" parameterType="Board">
        UPDATE BOARD
        SET TITLE = #{title}, CONTENT =  #{content}, EDITDATE = CURRENT_TIMESTAMP()
        WHERE BNUM = #{bnum} AND NAME = #{name} AND PASSWORD = #{password}
    </update>

    <delete id="deleteBoard" parameterType="Board">
        DELETE FROM BOARD
        WHERE BNUM = #{bnum} AND PASSWORD = #{password}
    </delete>

</mapper>